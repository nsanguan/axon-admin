apiVersion: v1
kind: Service
metadata:
  name: axon-admin-web
  namespace: axon-admin
  labels:
    app: axon-admin-web
spec:
  selector:
    app: axon-admin-web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
